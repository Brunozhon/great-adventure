// Minified version of the script at index.html
var scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,1,.1,1e3),renderer=new THREE.WebGLRenderer,geometry=new THREE.BoxGeometry,plantWorldMaterial=new THREE.MeshBasicMaterial({color:65280}),oceanWorldMaterial=new THREE.MeshBasicMaterial({color:65535}),plantWorld=new THREE.Mesh(geometry,plantWorldMaterial),oceanWorld=new THREE.Mesh(geometry,oceanWorldMaterial);function animate(){requestAnimationFrame(animate);renderer.render(scene,camera);plantWorld.rotation.x+=.01;plantWorld.rotation.y+=.01;oceanWorld.rotation.x+=.01;oceanWorld.rotation.y+=.01}scene.add(plantWorld);scene.add(oceanWorld);oceanWorld.position.x=2;oceanWorld.position.y=2;camera.position.z=5;renderer.setSize(500,500);document.body.appendChild(renderer.domElement);animate();
